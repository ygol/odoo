<?xml version="1.0" encoding="utf-8"?>
<odoo>

        <record id="view_users_form" model="ir.ui.view">
            <field name="name">res.users.form</field>
            <field name="model">res.users</field>
            <field name="inherit_id" ref="base.view_users_form"/>
            <field name="arch" type="xml">
                <xpath expr="//page[@name='Calendar']/group/group[@name='Microsoft Calendar']" position="inside">
                    <group string="Office 365 Calendar" name="Microsoft Calendar">
                        <group>
                            <field name="microsoft_calendar_rtoken" string="Refresh Token" readonly="1"/>
                            <field name="microsoft_calendar_token" string="User token" readonly="1"/>
                            <field name="microsoft_calendar_token_validity" string="Token Validity" readonly="1"/>
                            <field name="microsoft_calendar_sync_token" string="Next Sycn Token" readonly="1"/>
                        </group>

                            <button
                            string="Reset Account" class="btn btn-secondary"
                            type="action" name="%(microsoft_calendar_reset_account_action)d"
                            context="{'default_user_id': active_id}"/>
                    </group>
                </xpath>
            </field>
        </record>

</odoo>
